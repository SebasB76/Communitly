server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Flutter web es una sola pagina: toda ruta desconocida vuelve a index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
